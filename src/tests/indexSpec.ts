import { describe } from 'node:test';
import app from '../index';

describe(
    // it('test', async () => {
    //     const myfunc = await app.get('/');
    //     expect(myfunc).toBe(200);
    // });
);